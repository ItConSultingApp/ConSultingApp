<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cotion.it_consultiong">

    <uses-permission android:name="android.perdmission.INTERNET" />

    <application
        android:name=".UI.Sign.Dialog.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.It_Consultiong">
        <activity android:name=".UI.Main.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.FragmentMaInActivity">

        </activity>
        <activity android:name=".UI.Sign.SignInActivity" />
        <activity android:name=".UI.LoginMainActivity" /> <!-- <activity android:name=".UI.FragmentMaInActivity"/> -->
        <activity android:name=".UI.Sign.SignUpActivity" />
        <activity android:name=".UI.Main.BoardPostDrawUp" />
    </application>

</manifest>